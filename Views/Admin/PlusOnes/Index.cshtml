@model IEnumerable<PlusOne>
@{
    ViewData["Title"] = "Acompanhantes";
}

<div class="container py-5">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4">
        <div><h1 class="mb-1">Acompanhantes</h1><p class="text-muted mb-0">Gere os Plus One associados a cada convidado.</p></div>
        <a class="btn btn-primary" asp-action="Create"><i class="bi bi-person-plus"></i> Adicionar acompanhante</a>
    </div>
    <div class="table-responsive">
        <table class="table align-middle">
            <thead><tr><th>Acompanhante</th><th>Convidado associado</th><th class="text-end">Ações</th></tr></thead>
            <tbody>
            @foreach (var plusOne in Model)
            {
                <tr>
                    <td>@plusOne.PlusOneDisplayName</td><td>@plusOne.MainGuest?.DisplayName</td>
                    <td class="text-end"><a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@plusOne.Id">Editar</a>
                        <form method="post" asp-action="Delete" asp-route-id="@plusOne.Id" class="d-inline" data-delete-confirmation data-delete-message="Queres mesmo eliminar o acompanhante «@plusOne.PlusOneDisplayName»? Esta ação não pode ser anulada.">@Html.AntiForgeryToken()<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash3"></i> Remover</button></form></td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
