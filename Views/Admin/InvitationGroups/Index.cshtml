@model IEnumerable<Family>

<div class="container py-5">
    <h1>InvitationGroups</h1>
    <p>
        <a class="btn btn-primary" href="/Admin/InvitationGroups/Create">Create</a>
    </p>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>GroupCode</th>
                <th>AllowGroupRsvp</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.GroupCode</td>
                    <td>@item.AllowGroupRsvp</td>
                    <td>
                        <a href="/Admin/InvitationGroups/Edit/@item.Id">Edit</a>
                        <form method="post" action="/Admin/InvitationGroups/Delete/@item.Id" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button class="btn btn-sm btn-link text-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>